/**
 * Relays shown in the Connected Relays card row (NIP-11 info + status).
 */
export const DISPLAY_RELAYS = [
  'wss://relay.damus.io',
  'wss://relay.primal.net',
  'wss://nos.lol',
];

/**
 * Relays used as the live event stream source.
 * These are also the relays configured in the NPool (App.tsx).
 */
export const STREAMING_RELAYS = [
  'wss://relay.fizx.uk',
  'wss://relay.nostr.band',
  'wss://TheForest.nostr1.com',
];
